<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="Qna">
 <insert id="insertqna" parameterType="qna">
 <selectKey resultType="int" order="BEFORE" 
 keyProperty="boardNum">
 select nvl(max(boardnum),0)+1 from boards
 </selectKey>
 insert into boards(writeId, password, boardTarget, boardNum, secret, head, subject,
 content, uploaddate, reply) values (#{writeId}, #{password}, #{boardTarget}, #{boardNum}, #{secret}, #{head}, #{subject},
 #{content}, sysdate, #{reply})
 </insert>
 
 </mapper>